import React from 'react';
import './Header.css';

const Header = (props) =>
    <header className="main-header">
        <h1 className="main-header__title">FlexTimeTracker</h1>
    </header>;

export default Header;
